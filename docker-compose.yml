services:
  app:
    build: .
    ports:
      - "3000:3000"
    env_file:
      - .env
    volumes:
      - uploads:/app/public/uploads
      - db_data:/app/data
    restart: unless-stopped

volumes:
  uploads:
  db_data:
